{
  "name": "agents",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/agents/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "dependsOn": [
        {
          "dependencies": true,
          "target": "build"
        }
      ],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/apps/agents",
        "format": ["cjs", "esm"],
        "bundle": false,
        "main": "apps/agents/src/main.ts",
        "tsConfig": "apps/agents/tsconfig.app.json",
        "assets": ["apps/agents/src/assets"],
        "generatePackageJson": true,
        "esbuildOptions": {
          "sourcemap": true,
          "outExtension": {
            ".js": ".js"
          }
        }
      },
      "configurations": {
        "development": {},
        "production": {
          "esbuildOptions": {
            "sourcemap": false,
            "outExtension": {
              ".js": ".js"
            }
          }
        }
      }
    },
    "services": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "docker-compose -f apps/agents/docker-compose.yml up"
          }
        ]
      }
    },
    "db:sync": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "ts-node -P apps/agents/tsconfig.app.json apps/agents/src/scripts/sync_db.ts"
          }
        ]
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "agents:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "agents:build:development"
        },
        "production": {
          "buildTarget": "agents:build:production"
        }
      }
    }
  },
  "tags": []
}
