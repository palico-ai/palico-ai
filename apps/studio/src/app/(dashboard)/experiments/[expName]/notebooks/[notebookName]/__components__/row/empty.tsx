import { Typography } from '@palico-ai/components';
import React from 'react';

const EmptyRowWidget: React.FC = () => {
  return (
    <Typography variant="subtitle2" textAlign={'center'} p={2}>
      Get started by selecting a widget
    </Typography>
  );
};

export default EmptyRowWidget;
